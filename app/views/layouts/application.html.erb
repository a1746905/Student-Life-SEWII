<!DOCTYPE html>
<html>
	<head>
		<title>Student Life | University of Adelaide</title>
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>
		<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
		<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
		<%= csrf_meta_tags %>

		<!-- tried to implement in a ruby on rails way but haven't managed to get it working yet-->
		<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css">
		
		<!-- i want to move the following script and link but not sure what file or how to link
				 i've added it to public as side.js but not sure if theres an existing application
				 file? i've tried but couldn't get it working so keeping this here for now -->
		<script>
			function openNav() {
				document.getElementById("side").style.width = "250px";
				document.getElementById("main").style.marginLeft = "250px";
			}
			function closeNav() {
				document.getElementById("side").style.width = "0";
				document.getElementById("main").style.marginLeft = "0";
			}
		</script>
	</head>
	<body>
		<div id="side" class="nav_side">
			<a href="javascript:void(0)" class="close" onclick="closeNav()">&times;</a>
			<h2>Navigation</h2>
			<a href="#">Payment</a>
			<a href="#">To Do List</a>
			<a href="#">Reminders</a>
			<a href="#">What's On</a>
		</div>

		<div id="main">
		<div class="nav_top">
			<a onclick="openNav()" class="icon question open"><i class="fas fa-bars"></i></a>
			<%= link_to homepage_path, class: 'icon' do %><% image_tag 'logo.png', width: 52 %><% end %>
			<%= link_to "Home", homepage_path %>
			<%= link_to "Courses", coursePage_path %>
			<a href="#">Calendar</a>
			<a href="#">Ask Adelaide</a>
			<%= link_to helpPage_path, class: 'icon question' do %><i class="fas fa-question-circle"></i><% end %>

			<div class="nav_drop" style="float:right;">
				<a class="user"><i class="fas fa-user"></i></a>
				<div class="drop">
					<a href="#">Settings</a>
					<a href="#">Log Out</a>
				</div>
			</div>

			<div class="search">
				<form>
					<input type="text" placeholder="Search ..." name="search">
					<button type="submit"><i class="fa fa-search"></i></button>
				</form>
			</div>

		</div>
		<div id="body"><%= yield %></div>
		</div>
	</body>
</html>
